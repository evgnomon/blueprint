- name: Gather OS specific Vars
  hosts: localhost
  gather_facts: true
  tasks:
    - include_tasks: "tasks/setup_vars.yaml"

- name: zuul roles
  hosts: localhost
  roles:
    - role: zull
  environment:
    PATH: "{{ custom_path }}"
