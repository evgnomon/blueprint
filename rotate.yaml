---
- name: Include Charts
  hosts: all
  gather_facts: false
  roles:
    - charts
  tags:
    - always

- name: Update GitHub secrets
  hosts: localhost
  roles:
    - name: github_secret
      vars:
        secrets: "{{ secrets }}"
