- name: Set OS specific Vars
  hosts: localhost
  gather_facts: true
  tasks:
    - include_tasks: "tasks/setup_vars.yaml"

- name: Vim role
  hosts: localhost
  roles:
    - role: vim
  environment:
    PATH: "{{ custom_path }}"
