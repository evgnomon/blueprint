{% set arch = 'amd64' if ansible_architecture == 'x86_64' else ansible_architecture %}
deb [arch={{ arch }} signed-by=/etc/apt/trusted.gpg.d/hashicorp-archive-keyring.gpg ] https://apt.releases.hashicorp.com {{ ansible_distribution_release }} main
