---
user_fonts_dir: ~/Library/Fonts/
fish_bin_prefix: "{{ ansible_env.HOME }}/.macports/bin"
cargo_cmd: "{{ ansible_env.HOME }}/.cargo/bin/cargo"

macports_packages:
  - fish
  - nnn
  - alacritty
  - rsync
  - ctags
  - hugo
  - gnupg2
  # This requires XCode
  - pinentry-mac
  - tree
  - the_silver_searcher

cargo_packages:
  - alacritty
