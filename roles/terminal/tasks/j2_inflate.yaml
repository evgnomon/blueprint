---
- file:
    state: directory
    path: "{{ ansible_env.HOME }}/{{ item.path | replace('.j2', '') | dirname }}"
- template:
    src: "{{ item.src }}"
    dest: "{{ ansible_env.HOME }}/{{ item.path | replace('.j2', '') }}"
