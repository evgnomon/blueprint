- name: localhost roles for Ubuntu
  hosts: localhost
  tasks:
    - name: Include pkg role
      with_items: "{{ pkg.charts }}"
      include_role:
        name: pkg
      loop_control:
        loop_var: package
  roles:
    - terminal
    - vim
    - docker
    - azure_cli
  environment:
    PATH: "{{ custom_path }}"
