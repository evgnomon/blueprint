- name: localhost roles for Ubuntu in server
  hosts: localhost
  tasks:
    - name: Include pkg role
      with_items: "{{ pkg.charts }}"
      include_role:
        name: pkg
      loop_control:
        loop_var: package
  roles:
    - terminal
    - vim
    - azure_cli
  environment:
    PATH: "{{ custom_path }}"
